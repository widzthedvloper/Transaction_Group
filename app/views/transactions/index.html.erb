<div>
  <h2>TRANSACTIONS</h2>
</div>
<div>
<spam>Total Amount</spam>
<spam>$<%= @sum %></spam>
</div>
<% @transactions.each do |transactions| %>
  <div>
    <div>
     <%= image_tag('transactions.png') %>
    </div>
    <div>
      <p><%= transactions.name %></p>
      <p><%= transactions.created_at %></p>
    </div>
    <div>
     <p>$<%= transactions.amount %></p>
    </div>
  </div>
<% end %>
<%= link_to 'Create Transaction', create_transaction_path %>